[project]
name = "ha-energy-manager"
version = "1.0.1"
description = "Home Assistant custom integration for EcoFlow battery management"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.ruff]
target-version = "py311"
line-length = 100
